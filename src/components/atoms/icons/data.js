// Svg
import { svg } from './svg'

// Theme
import { theme } from '../../../styles/theme'

export const icons = {
  burgerMenu: {
    name: 'burgerMenu',
    src: teste.burgerMenu,
    color: theme.icons.color,
    size: theme.icons.size
  }
}
